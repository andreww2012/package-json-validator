export const emailFormat = /\S+@\S+/; // I know this isn't thorough. it's not supposed to be.
export const packageFormat = /^[/0-9@a-z][\w\-./@]*$/i;
export const urlFormat = /^https?:\/\/[-.0-9a-z]+/;
export const versionFormat = /^\d+\.\d[+\-.0-9a-z]+$/i;
